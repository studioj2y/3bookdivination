# 游戏 UI 互动特性评审 · 超级银河自动算命机高级版II型强化式样

> 主模式：`game-ui`（游戏 UI 视觉/交互规范）｜支持模块：`game-ui-motion`（交互动效）
> 评审对象：`export-2026-08-31/index.html`（单文件，2300 行，APP_VERSION `v1.0.0`）
> 评审日期：2026-09-16｜评审角色：独立审稿（不与创作者共享自我解释）
> 视角：**用游戏 UI 的互动标准量一个 Web 工具** —— 即时反馈、状态可见、引导不断链、触控优先、可中断、可降级。

---

## 1. 结论

**总评：视觉氛围与「仪式感节奏」是明确的强项；短板全部集中在「交互层」——反馈缺口、状态覆盖不全、移动端互动断链、动效降级缺失。**

分项判定（`通过 / 有条件通过 / 不通过`）：

| 维度 | 判定 | 一句话理由 | P0 | P1 | P2 |
|---|---|---|---|---|---|
| 动效 Motion | **有条件通过** | 分层意图清楚（即时反馈 vs 场景过渡），但 0 条中断规则、0 条 reduce 降级、0 份节奏表 | 1 | 5 | 2 |
| 布局 Layout | **有条件通过** | 桌面成立；移动端八字/六爻双层滚动、无安全区适配、触控目标偏小 | 2 | 3 | 2 |
| 引导 Onboarding | **不通过** | 首屏黑屏无反馈、书入场期「隐形可点」、未开放功能无降级态 | 3 | 3 | 2 |
| 要素 Elements | **有条件通过** | 组件状态最全的是塔罗牌（可作样板），但可点元素大量用 `div`、状态色令牌不统一 | 1 | 4 | 3 |
| **合计 31 项** | | | **7** | **15** | **9** |

**最有价值的单点改造**：`.tcard` 的六态组件（默认/active/locked/shake/flipped/breathing）已经是全站最好的交互样板，把它抽象成通用组件契约并推广到书、按钮、下拉、面板，能一次性消掉一半问题。

---

## 2. 依据

| 类别 | 来源 | 证据状态 |
|---|---|---|
| Brief（产品意图） | `PROJECT_GUIDE.md` §0/§1/§3/§4 | `已验证`（文件可读，但 §4 部分参数与实现漂移，见 D6） |
| 参考规范 | `design4game` → `game-ui-art-direction.md` + `game-ui-motion.md` | `已验证`（本次会话加载） |
| 项目证据 | `index.html` 全量 CSS/JS/结构，逐条标注行号 | `已验证` |
| 运行时证据 | 未在真机/模拟器上实测动效帧率、触控、后台恢复 | **`待验证`** —— 本文所有动效体感判断均为静态代码推导 |
| Art Bible | 不存在。本项目从未建立视觉/交互规则文件 | `已验证`（缺失） |

> ⚠️ 本文不含真机截图或录屏证据。标注 `待验证` 的条目需要在目标设备上补验后才可标 `通过`。

---

## 3. 现状盘点

### 3.1 五段交互链路

```
loading ──(图片加载 / 2s 兜底)──▶ intro ──(点击任意处)──▶ enter ──(自动 p≥1)──▶ field ──(+1500ms)──▶ books
                                                                                        │
                                                                    点击书 ▼            │ [重播]
                    ┌──────────────┬──────────────┬──────────────┬──────────────┐
                    │  idx 0 八字   │  idx 1 占星   │  idx 2 塔罗   │  idx 3 六爻   │
                    │  表单→排盘    │  空壳占位     │  选阵→抽→逐张翻→终章 │  摇 6 次→装卦 │
                    └──────────────┴──────────────┴──────────────┴──────────────┘
                                        │ [返回书架] 回到 books
```

### 3.2 层级现状（z-index）

| 层 | z | 交互 | 备注 |
|---|---|---|---|
| `#table` | 0 | none | 暗色台面 |
| `#gl` | 0（CSS 写 2，`startField` 覆盖为 0，`:1016`） | none | 粒子场 |
| `#books` | 1 | `none → auto`（`.show`，`:33`） | 三本书 |
| `#ui` | 6 | none | 魔杖 + 暗角 |
| `#hint` / `.btn`(`#replay`,`#skip`) / `#funcpage` | 8 | — | **同层** |
| **`#ver`** | **9** | none | **版本号压在功能页之上（B1）** |
| `#fx` | 20 | none | 终章爆炸 |

> 与 `PROJECT_GUIDE.md` §3/§4.2 的「书本(5) < 魔杖(6) < 光粒(7) < 按钮/功能页(8)」**已不一致**：实现是「光粒(0) 在书(1) 之后」。视觉上更易读，但文档口径需拍板（B6）。

---

## 4. 分项评审

### 4.1 引导 Onboarding —— **不通过**

| ID | 级别 | 问题 | 证据 | 建议 |
|---|---|---|---|---|
| **A1** | P0 | 首屏纯黑无反馈：`loading` 阶段只清画布不出任何元素（`:1334-1336`），直到图片就绪或 2s 兜底（`:985`）。慢网/大图下最长 2 秒黑屏，用户不知道在加载 | `index.html:1334-1336, 985` | 加载态给一枚「呼吸光点 + 一行文案」，与 intro 共用同一氛围语言 |
| **A2** | P0 | **书菜单入场期间「隐形可点」**：`#books.show` 一加即 `pointer-events:auto`（`:33`），而 `.bookWrap` 此刻 `opacity:0`（`:34-36`），入场 1.2–1.4s、标签再延后 0.9s。入场期间书看不见但**可被点中并直接跳转** | `:31-36, 1358-1362` | 入场未结束时保持 `pointer-events:none`，由 JS 在入场结束（或 `animationend`）后加 `.ready` 再放开 |
| **A3** | P0 | 书二「占星」无「未开放」态：`selectBook` 的 `else` 分支只渲染标题「占星」+「占星（开发中）」（`:1382-1390`），但书本身外观与已实现的三本**完全一致**（`:516-566`） | `:1382-1390` | 未实现的书降饱和/蒙灰 + 角标「未开放」，点击给提示而非进入死页 |
| **A4** | P1 | 校验反馈不指向字段：未选日期只写 `bzInfo` 文本（`:1573-1574`），不聚焦、不高亮、不滚动 | `:1572-1574` | `aria-invalid` + 描边变色 + `focus()` |
| **A5** | P1 | 错误态无统一组件、无重试：三处均为 `xxxInfo.textContent = '…失败：'+e.message`（`:1739`、`:2038`、八字同） | `:1739, 2038` | 统一错误组件 = 图标 + 文案 + 「重试」按钮 |
| **A6** | P2 | 优质引导语只存在于塔罗：`tpGuide` 的「你终于找到这里了啊……」+ 分步指引（`:1960`、`:2101`）质量很高；八字/六爻只有静态 label 与一行 `lyTossTxt`（`:1690`） | `:1960, 2101, 1690` | 把 `tpGuide` 抽为共用 `.guide` 组件，三本书共用 |
| **A7** | P1 | **引导期光标无提示**：`startIntro` 设 `glCanvas.style.cursor='pointer'`（`:1003`），但 `#gl` 全局 `pointer-events:none`（`:11`），该元素无法被 hover ⇒ 光标样式永不生效。首屏用户看不到「这里可以点」的手型反馈 | `:11, 1003` | 在 `body`/`#table` 层承载引导期光标，或临时放开 `#gl` 的 pointer-events |
| **A8** | P2 | `#hint`「轻触水晶球，进入未知」延迟 1800ms 才淡入（`:1004`），与 2s 的 intro 时长叠加后，提示出现的时间窗偏窄 | `:1004, 969` | 提前到 ~900ms，或与水晶球一起淡入 |

### 4.2 布局 Layout —— **有条件通过**

| ID | 级别 | 问题 | 证据 | 建议 |
|---|---|---|---|---|
| **B1** | P0 | **版本号压在结果页之上**：`#ver{z-index:9}`（`:19`）> `#funcpage{z-index:8}`（`:52`），右下角 `v1.0.0` 会浮在八字/塔罗/六爻结果页表面 | `:19, 52` | 建立层级令牌；`#ver` 降到 7 或并入 `#funcpage` |
| **B2** | P0 | **移动端双层滚动**：`#funcpage` 在 ≤720px 已成为滚动容器（`:400`），但 `#baziPanel`/`#liuyaoPanel` 仍保留 `max-height:86vh; overflow-y:auto`（`:64`、`:155`）——只有塔罗面板被移动端规则显式解锁（`:401`）。结果：手机上八字/六爻是「面板内滚 + 页面滚」两层，手势易滚错层、面板底部够不到 | `:400, 64, 155, 401` | 移动端三面板统一 `max-height:none; overflow:visible`，只保留 `#funcpage` 单层滚动 |
| **B3** | P1 | 无安全区适配：`viewport-fit=cover` 已声明（`:5`），但全站 0 处 `env(safe-area-inset-*)`；`.btn{top:14px}`、`#ver{bottom:8px}`、`#hint{bottom:9%}` 均为死值 | `:5, 19-27` | 按 game-ui 决策表「按动态安全区布局，避免写死边距」 |
| **B4** | P1 | 触控目标不足 44×44：`.btn` 高约 28px（`:23-25`）、`.bz-lynav button` 约 26px（`:122-123`） | `:23-25, 122-123` | ≤720px 下最小 44px，或用伪元素扩大热区 |
| **B5** | P2 | 功能页背景半透明（`:53`，alpha .93），与 `#replay` 同为 z8 且 DOM 在后 ⇒ 重播按钮可能微微透出 | `:53, 52` | 不透明底，或在 `.func` 态隐藏全局按钮 |
| **B6** | P2 | `#gl` 层级与文档相反（实现「光粒在书后」，文档「光粒在书前 z7」）。`startField` 内有注释解释原因（`:1014-1016`），但 `PROJECT_GUIDE` §4.2 未同步 | `:1014-1017` vs `PROJECT_GUIDE.md §4.2` | 二选一并同步文档：改口径（推荐，可读性更好）或恢复层级 |
| **B7** | P1 | 塔罗移动端自成 74vh 滚动视口（`:407-413`）叠加页面滚动，且内部是横向网格 —— 嵌套滚动是已知代价，注释已说明是为此前的「释义显示不全」让路 | `:406-413` | 保留，但明确「牌阵内只横滚、不纵滚」；或改回整页滚动 + 释义改为点击展开 |

### 4.3 要素 Elements —— **有条件通过**

| ID | 级别 | 问题 | 证据 | 建议 |
|---|---|---|---|---|
| **C1** | P0 | **可点元素大量是 `div`**：`.book`（`:457`，click 绑在 `:2288`）、`.tp-select-item`（`:1935`）、`.tcard`+`onclick`（`:2075`）⇒ 键盘不可达、无 focus、无 role | `:457, 1935, 2075, 2288` | 书改 `<button>`；下拉项补 `role="option"`+`tabindex`+方向键；卡牌补 `tabindex="0"`+`role="button"`+Enter/Space |
| **C2** | P1 | 全站 31 处 `transition/animation`，**0 处 `:focus-visible`、0 处 `:active`**（除 `.ly-coin`）。「按下」这一最基础的即时应答缺失 | 全站 | 补统一 focus 环 + `:active{transform:scale(.98)}` |
| **C3** | P1 | 状态色令牌不统一：`t-good` `#8fd7b3`（`:118`）／`ly-flag.good` 同色但独立定义（`:249`）／`orient.up` `#7ff0b8`（`:337`）／`tcard.active` 金 `#f3d97a`（`:346`）／`ly-slot.mv` 橙 `rgba(200,140,60,…)`（`:184`）／`ly-tag.mv` `#f0c48d`（`:243`）。同一语义在不同组件取值不同 | 多处 | 抽 8 个令牌：`--s-good/--s-bad/--s-mid/--s-accent/--tx-hi/--tx-mid/--tx-low/--s-border`，全套色值改为引用 |
| **C4** | P1 | 六爻「进阶规则」的释义只挂在 `title` 属性（`:1754`）+ `cursor:help`（`:248`）⇒ **移动端完全读不到**（无 hover）。这恰是六爻的差异化能力 | `:248, 1754` | 点击/长按弹 popover，或在爻行下方直接列出规则名+一句解释 |
| **C5** | P1 | 组件状态覆盖极不均：最全的是 `.tcard`（默认/`active`/`locked`/`shake`/`flipped`/`breathing`，`:344-350`、`:386`）；`.book` 仅 2 态（`,37-40`）；`.tp-btn` 有 `disabled`（`:305`）无 loading；`.back` 无 disabled/focus | 多处 | 以 `.tcard` 为样板，全站组件补「默认／反馈／不可用／异常」四态 |
| **C6** | P2 | `maximum-scale=1, user-scalable=no`（`:5`）封死放大路径，低视力用户无法放大结果页小字（`10px`/`10.5px` 的字号在 `:112`、`:1646` 等大量出现） | `:5` | 至少放宽为 `maximum-scale=5` |
| **C7** | P2 | 三本书各内联完整 SVG（每本约 60 行，`:458` 起），结构与 defs 高度重复 | `:457-` | 按 `PROJECT_GUIDE` §7 换为 `assets/book-N.png/svg` 外链，同时降首屏体积 |
| **C8** | P2 | 五行配色映射在两处各自硬编码：`WX_COLOR`（`:1413`）与 `LY_WX`（`:1610`）内容完全相同 | `:1413, 1610` | 合并为单一常量 |

### 4.4 动效 Motion —— **有条件通过**

| ID | 级别 | 问题 | 证据 | 建议 |
|---|---|---|---|---|
| **D1** | P1 | `prefers-reduced-motion` **只覆盖 JS**（`:968-970`、`:1183`、`:1240-1245`）；CSS 动画全部未降级：`bookFloat`（`:50`）、`spin`/`pileBreathe`（`:330-331`）、`cardBreath`（`:386`）、`pulseBtn`（`:347`）、`lyTumble`（`:195-201`）、`menuIn`（`:298`） | 多处 | 加全局 reduce 兜底（animation/transition duration → 0.01ms），再为需要保留氛围者单独给静态替代 |
| **D2** | P0 | **终章闪白 + 320 粒子爆炸无任何降级**：`startFinale`（`:2173-2198`）与 `drawFinale`（`:2199+`）完全不读 `reduce`；全屏白闪 1.1s，峰值叠两层白（`:2211`、`:2217`）。光敏敏感风险、低端机掉帧，且 1.1s 内整屏信息被淹没、无跳过入口 | `:2173-2219` | ①`reduce` 下降级为静态淡入 + 呼吸光；②正常态峰值降到 ~0.35、时长缩到 ~700ms；③给「跳过特效」 |
| **D3** | P1 | **快速连点缺守卫**（不一致）：八字 `bzGo.onclick` 无 in-flight 守卫（`:1572`）⇒ 连点发多次请求、结果乱序覆盖；塔罗 `tpDraw.onclick` 无守卫（`:2020`）⇒ 连点会 `tarotBoard.innerHTML=''` **把已翻开的牌清空重来**。而六爻有 `lyBusy`（`:1712`）、翻牌有 `flipped/active` 双守卫（`:2115-2116`） | `:1572, 2020 vs 1712, 2115` | 统一 in-flight 守卫模式（禁用按钮 + 早返回），以六爻为样板 |
| **D4** | P1 | **后台恢复无补偿**：全程用绝对时间 `t - phaseStart`（`:1176`、`:1206`、`:1241`、`:2200`），且全站未监听 `visibilitychange`。切后台再回来时间跳变 ⇒ enter 阶段瞬间跳到 `p≥1` 直接进 field（`:1235`）；终章瞬间走完 | `:1176, 1206, 1235, 2200` | 改为累计 delta 的时钟，或监听 `visibilitychange` 重置 `phaseStart` |
| **D5** | P1 | **无成文节奏表**：31 处时长散落 180ms（menuIn）→ 720ms（coin）→ 800ms（flip）→ 1100ms（闪白）→ 1500ms（glow）→ 2400ms（field）→ 3100ms（enter）。分层意图可读，但没有缓动类别/中断/降级/责任人的书面约定 | 全站 | 采纳 §5.2 的动效节奏表 |
| **D6** | P2 | 漂浮幅度与文档不符：文档 `bookFloat 6s`、`0↔-12px`；实现 `7s`、`0↔-4px`（`:50`）——漂浮只剩 1/3，「浮」的氛围被削弱 | `:50` vs `PROJECT_GUIDE §4.1` | 拍板：修文档或修实现（建议改回 -12px 并同步文档） |
| **D7** | P2 | `lyTossOnce` 双轨计时：CSS 动画 720ms 与 `await lySleep(300+470)` 并行（`:1670-1676`）。切后台时 CSS 动画暂停而 `setTimeout` 继续 ⇒ 落面与动画错位 | `:1670-1676` | 统一由一方驱动（动画结束事件 or JS 计时 + 无 CSS 动画） |
| **D8** | P1 | **移动端核心互动缺失**：魔杖/光粒照亮依赖 `mouse.active`，而触摸监听绑在 `#gl`（`:995-997`）——`#gl` 恒为 `pointer-events:none`（`:11`、`:1017`），触摸事件永远不会到达；`#books` 显示时又铺满全屏吃掉触摸。⇒ 手机上「光随指尖照亮」这个卖点完全不可用 | `:11, 995-997, 1017` | 把触摸监听改绑 `window`（与鼠标一致），并在移动端给指尖光晕一个常驻的弱版本 |

---

## 5. 生产规格（建议落地的规范）

### 5.1 层级令牌（消解 B1/B5/B6）

| 令牌 | z | 用途 |
|---|---|---|
| `--z-table` | 0 | 台面 |
| `--z-field` | 0 | 粒子场（与 table 同层，DOM 序在后） |
| `--z-books` | 1 | 书菜单 |
| `--z-wand` | 6 | 魔杖 + 暗角 |
| `--z-hud` | 7 | 版本号 / 全局按钮（**从 8/9 降下来**） |
| `--z-page` | 8 | 功能页与结果面板 |
| `--z-pop` | 60 | 下拉、popover、tooltip |
| `--z-fx` | 90 | 终章全屏特效（**从 20 提到最上但保持 pointer-events:none**） |

### 5.2 动效节奏表（消解 D5）

| 事件 | 功能 | 时长 | 曲线 | 中断 | 输入策略 | 降级 |
|---|---|---|---|---|---|---|
| 悬停/按下 | 即时反馈 | 90–140ms | `ease-out` | 可打断 | 不阻塞 | 无 |
| 下拉展开 | 即时反馈 | 180ms | `ease-out` | 可打断 | 不阻塞 | 无 |
| 铜钱翻滚 | 仪式反馈 | 720ms | `cubic-bezier(.25,.7,.35,1)` | 不可打断 | **阻塞按钮** | reduce → 直出结果 + 文字 |
| 翻牌 | 反馈 + 连续性 | 800ms | `cubic-bezier(.2,.8,.2,1)` | 可打断 | 不阻塞 | reduce → 直出 |
| 释义面板滑入 | 连续性 | 500ms | `cubic-bezier(.2,.8,.2,1)` | 可打断 | 不阻塞 | reduce → 淡入 |
| 书入场 | 层级出现 | 1200–1400ms | `cubic-bezier(.2,.8,.2,1)` | — | **入场期间必须 `pointer-events:none`（A2）** | reduce → 直接显示 |
| 场景过渡（enter/field） | 定向 + 连续性 | 2400–3100ms | `p²` | 可被「跳过」中断 | 允许跳过 | reduce → 500/2000ms（已有） |
| 终章特效 | 奖励强调 | ≤700ms | 快起慢收 | **需可跳过** | 不阻塞领取 | reduce → 静态淡入（D2） |

### 5.3 组件状态矩阵（消解 C5）

每个交互组件至少四态：**默认 / 反馈(hover+active+focus) / 不可用 / 异常**。

| 组件 | 默认 | hover | active | focus | disabled | loading | 异常 |
|---|---|---|---|---|---|---|---|
| `.book` | ✓ | ✓ | ✗ | ✗ | 需补（未开放，A3） | — | — |
| `.btn`（重播/跳过） | ✓ | ✗ | ✗ | ✗ | — | — | — |
| `.tcard` | ✓ | ✓ | ✗ | ✗ | ✓`locked` | — | ✓`shake` |
| `.tp-btn` | ✓ | ✓ | ✗ | ✗ | ✓ | ✗ | ✗ |
| `.ly-toss` | ✓ | ? | ✗ | ✗ | ✓ | ✗ | ✗ |
| `.ly-coin` | ✓ | — | ✓`tumble` | — | — | ✓`tumble` | — |

> `.tcard` 是唯一接近完整的组件，作为全站样板。

### 5.4 移动端硬约束（消解 B2/B3/B4/D8）

1. ≤720px：三面板（八字/塔罗/六爻）统一 `max-height:none; overflow:visible`，滚动只由 `#funcpage` 承担。
2. 所有固定定位元素（`.btn`、`#ver`、`#hint`）改用 `env(safe-area-inset-*)` 加安全区补偿。
3. 所有可点目标 min 44×44（可用伪元素扩热区）。
4. 触摸事件绑 `window`，不再依赖 `#gl`。

---

## 6. 风险与降级

| 风险 | 影响 | 现状 | 降级路径 |
|---|---|---|---|
| 未在真机验证 | 动效体感、帧率、触控结论均可能失真 | 本文所有 D 类判断为静态推导 | 用 CDP/真机跑一遍五段链路，录屏后再回填判定 |
| 单文件 2300 行 | 继续叠加会加速腐化；`PROJECT_GUIDE` §1 已计划迁 PixiJS+GSAP+Vue3 | 当前仍可维护 | 至少先把 §5 的令牌抽成 CSS 变量块，为迁移铺路 |
| 文档与实现漂移 | B6/D6 两处已知不一致，后续接手者会被带偏 | 已知 | 每轮改动同步 `PROJECT_GUIDE.md` §4 |
| 无 Art Bible | 三本书各自定义状态色，漂移会持续累积 | 缺失（C3 已量化） | 把 §5 落成 `UI_SPEC.md`，纳入版本管理 |

---

## 7. 验收清单

| 检查项 | 首评结果 | 复验（2026-09-16，第一批+第二批已落地） | 未通过动作 |
|---|---|---|---|
| 关键任务在最小目标屏（≤420px）无需猜测即可完成 | **不通过** | **有条件通过** —— B2/B4/A3/B8 已在 390×844 实测通过；A4/A5 错误态仍未做 | 补 A4/A5 后转「通过」 |
| 每个交互组件覆盖默认/反馈/不可用/异常 | **不通过** | **不通过** —— 属第三批（C5），未动 | 按 `.tcard` 样板补齐 |
| UI 与背景在明暗极值下仍分层 | **通过** | **通过** | — |
| 动效能解释状态变化且不延误主要任务 | **有条件通过** | **有条件通过** —— D2 闪白吞 UI、D3 连点清空属第三批；第一批已消掉 A9/B10 两处「无反馈/被吞点击」 | 修 D2/D3 |
| 快速连点 / 返回 / 断网恢复 / 减少动态均有确定结果 | **不通过** | **不通过** —— D1/D2/D3/D4 属第三批；「返回」路径已由 B9 收敛为幂等 | 四项分头修 |
| 真实设备录屏无卡顿闪跳 | **`待验证`** | **`待验证`** —— 本轮为 headless Chrome 1440×900 / 390×844，非真机 | 补真机录屏 |

---

## 8. 下一步（建议分三批，按性价比排序）

> **执行状态（2026-09-16）：第一批 ✅ 已完成；第二批 ✅ 已完成（并补做 4 项新发现）。第三批未开始。**
> 复验：`node tools/uicheck.mjs` —— 当时 **22 项全部 PASS / 0 FAIL，无运行期 JS 异常**。详见 §9。
> **脚本项数已增至 32 项（32 PASS / 0 FAIL）** —— 同日第二轮补入了六爻「一二三档」能力的断言，详见 §10；第四批门槛（D1/D2/D3/D4/C1/C2/C3/D5）仍未开始。

**第一批 · 低成本高收益（约半天）** ✅
B1 版本号降层 ｜ A2 入场 `pointer-events` ｜ A3 未开放书降级 ｜ A7 引导期光标 ｜ C8 合并五行常量 ｜ C6 放开缩放
→ 全部是单点改动，不触碰动效与布局骨架。

**第二批 · 移动端可用性（约一天）** ✅
B2 三面板单层滚动 ｜ B3 安全区 ｜ B4 触控 44px ｜ D8 触摸绑 window ｜ C4 进阶规则 popover
→ 这批决定「手机上能不能正常用」。

**第三批 · 规范与健壮性（约一天半）** ⬜ 未开始
D2 终章降级 ｜ D1 CSS reduce 兜底 ｜ D3 统一 in-flight 守卫 ｜ D4 后台恢复 ｜ C1/C2 键盘与 focus ｜ C3 状态色令牌 ｜ D5 节奏表落成文档
→ 这批是「从能用到专业」的分界。

每批完成后按 §7 回填验收结论；`待验证` 项必须在真机补验后才可改判。

---

## 9. 第一批 + 第二批 执行记录（2026-09-16）

### 9.1 落地清单

| ID | 改动 | 关键实现 |
|---|---|---|
| B1 | `#ver` 由 `z-index:9` → `7` | 版本号不再压在 `#funcpage`(8) 之上；同时接入右下安全区 |
| A2 | `.bookWrap` 默认 `pointer-events:none`，JS 在 `#books.ready` 后才放开 | `armBooksClickable()` 用 `BOOKS_READY_MS=2700`（对齐 `--d:1.2s` + `opacity 1.4s`）；返回书架会重新计时 |
| A3 | 占星书 `locked` + 「未开放」角标 + 点按只弹轻提示 | 新增 `#toast` 轻提示组件与 `showBookNotice()`；`selectBook(1)` 早返回 |
| A7 | 引导期光标由 `#gl` 改挂 `document.body` | `#gl` 恒 `pointer-events:none`，其 `cursor` 永不生效 |
| C6 | viewport 去掉 `maximum-scale=1, user-scalable=no`，改 `maximum-scale=5` | 低视力用户可放大结果页小字 |
| C8 | `LY_WX` 改为复用 `WX_COLOR` | 删掉一份重复硬编码 |
| B2 | ≤720px 三面板统一 `max-height:none; overflow:visible` | 滚动只由 `#funcpage` 承担；`.bz-lylist` 同步解除内滚 |
| B3 | `#ver` / `#hint` / `.btn` / `#skip` / `#replay` / `#toast` 接入 `env(safe-area-inset-*)` | `#funcpage` 移动端 padding 也加安全区 |
| B4 | ≤720px `.btn` / `.back` / `.tp-btn` / `.tp-select-btn` / `.bz-lynav button` / 表单控件 `min-height:44px` | 实测：排盘 44 / 流年翻页 44 / 返回书架 44 |
| D8 | 触摸监听由 `#gl` 改绑 `window`（含 `touchcancel`） | 实测触摸 (120,500) → `mouse.active=true` 且坐标一致 |
| C4 | `.ly-flag` 由 `span[title]` 改 `<button data-desc>`，`#lyResult` 上用事件委托弹释义 | 移动端无 hover，`title` 完全读不到 |

### 9.2 本轮新发现（原 31 项之外，均在第二批射程内）

| ID | 级别 | 问题 | 实测证据 | 状态 |
|---|---|---|---|---|
| **A9** | P1 | **「跳过引导」后首屏提示文字常驻** —— `startField()` 未清 `#hint`（只有 `startEnter()`/`replayAll()` 清），提示文字压在书菜单与结果页之上 | `#hint` opacity 全程 `0.9`；已在 `startField()` 补 `hint.style.opacity='0'` | ✅ 已修 |
| **B8** | **P0** | **手机 390px 下四本书溢出屏幕，首尾两本点不到** —— `4×118px + 3×24px = 544 > 390`，`flex-wrap:nowrap` 且无滚动，`justify-content:center` 把首尾挤出屏外 | 修前：八字书 `left=-77`、六爻书 `right=467`（超出 77px）；修后四本 `left/right` 全在 `[0,390]` 内，`scrollWidth == clientWidth == 390` | ✅ 已修 |
| **B9** | P1 | **两个功能页可同时叠在 `#funcpage` 内** —— `selectBook` 只在自身的 `else` 分支移除面板，任意叠加路径都会留下两个面板；叠加后 `justify-content:center` + 桌面无 `overflow` 会把内容顶端推出屏外，按钮漂到可视区之外 | 修复前实测到「六爻面板 + 塔罗面板」同屏；已新增 `closeAllPanels()` 作为唯一显隐入口，`selectBook`/`backToBooks`/`replayAll` 三处共用；复验进入任一功能页都只有 1 个面板 | ✅ 已修 |
| **B10** | **P0** | **面板刚打开的头 ~250ms 内，点击被「已淡出的书」吞掉** —— `#funcpage{transition: opacity .8s, visibility .8s}` 中 `visibility` 是离散属性，**显示时被延迟**；期间 `#funcpage` 不可命中，而 `#books` 自己的 `visibility` 过渡尚未走完、`.ready` 又让 `.bookWrap` 保持 `pointer-events:auto` ⇒ z-index 更低的隐形书赢下命中测试 | 采样：`+0ms` 命中 `text > g > svg > DIV.book`，`func{vis:hidden}`；`+250ms` 才转 `visible`。修后 `+0ms` 即命中 `BUTTON#lyStart`。表现即「点开面板后第一次点按钮没反应」（桌面六爻首击、手机八字首击均复现） | ✅ 已修 |

> **B10 的修法是标准写法**：`transition: opacity .8s ease, visibility 0s linear .8s`（隐藏等淡出走完），`.show` 态改 `transition: opacity .8s ease, visibility 0s`（显示瞬时）。`#books` 同步改造。另外离开书架时撤销 `.ready`，不留「看不见却能点」的窗口。

### 9.3 复验方法与证据

- 脚本：**`tools/uicheck.mjs`**（零依赖，Node ≥21 内置 `WebSocket`；`UI_BASE` / `UI_CDP` 可覆盖；退出码 0/1 可当回归）。
- 视口：桌面 `1440×900` 与手机 `390×844`（`mobile:true` + 触摸模拟）。
- 结果：**22 PASS / 0 FAIL**，无运行期 JS 异常；**连跑两次结论逐项一致**。断言覆盖 A2/A3/A7/A9/B1/B2/B3/B4/B8/B9/B10/C4/C8/D8。
- **C4 已改为确定性驱动**（2026-09-16 补）。摇卦由 `lyRand()` 决定三枚铜钱（0背=老阴动 / 1背=少阳 / 2背=少阴 / 3背=老阳动），故**任意目标卦局都能反推出一串 18 次 `lyRand` 布尔值**。脚本先向后端探一个「必带标记」的卦局（实测产标记：乾为天 8 个、水火既济 8 个 + 4 组合、坤为地 5 个），再把它注入 UI 摇出来，并逐位核对 `lyYaos` 与期望一致。原先「随机摇 → 无标记就重摇」的写法有约 18% 概率摇出六爻皆静之卦，属**测试自身的不稳定**，不是产品缺陷。
- 截图：`_shots/01-books-desktop.jpg`、`02-toast-astrology.jpg`、`03-liuyao-result.jpg`、`04-mobile-bazi.jpg`、`05-mobile-books.jpg`。
- **尚未覆盖**：终章特效、翻牌 3D、AI 解读、真机录屏。安全区这条：Chrome 133 没有 `Emulation.setSafeAreaInsetsOverride`（本轮 Chrome 153 已支持，B3 直接走「注入 44/34 → 读计算样式」的像素级验证，实测 `#ver.bottom=42px`、`#skip.top=58px`）；旧版浏览器则退化为 CSSOM 断言（确认 7 条规则确实写了 `env(safe-area-inset-*)`）。

### 9.4 一条方法论

`B9`/`B10` 都不是静态读代码能发现的：**它们只在「交互时序」里成立**（面板叠加要两个入口都走到；B10 只有 ~250ms 窗口）。真正把它们逼出来的是「逐步 snapshot + 埋点 + `elementFromPoint` 祖先链」——即本次的定点探针手法。首评（纯静态推导）把它们判为「不存在」是合理的，但**不能停在静态结论**。

### 9.5 回归脚本自身的一个坑（值得单独记）

`clickSel()` 原本直接取 `getBoundingClientRect()` 的中心坐标去点，有两种**静默失效**：

1. **元素在折叠下方** —— 长结果页里「再摇一卦」按钮的坐标落在视口之外，`Input.dispatchMouseEvent` 点到空白处，既不报错也不生效；
2. **元素 `display:none`** —— `getBoundingClientRect()` 返回全 0，于是点到 `(0,0)`。

两者都会让断言「看着在测、其实没测」（C4 首次就是这样**假红**）。修法：`clickSel()` 先判可见性，不在视口内则 `scrollIntoView({block:'center'})`，并把 **0×0 明确判为失败**（而不是照点 `(0,0)`）；已可见的元素不加任何延时，以免削弱 B10 这类时序断言的严格度。**任何「点不到但不算错」的测试动作，都是回归噪声的来源。**


---

## 10. 六爻模块「一二三档」功能纵深 + 收尾修复（2026-09-16 同日第二轮）

> 这一轮不在 §1~§8 的 UI 评审清单内，而是**六爻模块本身的功能扩展**。立场不变：**只做装卦与事实提取，不断吉凶**（不做 AI 解卦）。

### 10.1 三档落地清单

| 档 | 能力 | 实现落点 |
|---|---|---|
| 一 | **一键摇卦** | `#lyQuick` 传 `yao:null`，后端 `auto_cast` 掷六次、跳过逐掷动画；结果页标「快速起卦 · 未逐掷铜钱」 |
| 一 | **术语全可点** | `LY_TERMS`（19 条）+ `lyTerm(code,text,cls)`；`<span role=button tabindex=0>`，点击/回车弹 `#toast`（移动端无 hover，`title` 读不到） |
| 一 | **一卦多断（同卦另断）** | 结果页 `.ly-recast[data-cat]` chip 换占事类别 → 同一组爻重取用神 |
| 二 | **应期 / 飞伏生克 / 用神两现取舍 / 独发独静 / 神煞（驿马·桃花）/ 卦爻辞原文** | `api/liuyao.py`：`shensha_of` / `yingqi_of` / `fufu_relation` / `fushen_note`，`yongshen.cands` + `reason`（舍空破→取临世应→取动→取旺）；`api/zhouyi.py`（64 卦 384 爻通行本，`self_check()` 自检） |
| 三 | **卦盘图（本卦/变卦对照 + 动爻连线）/ 导出长图 / 占卦本** | `lyBoardSVG`（viewBox 420×284，`.lb-anim`/`.lb-link` 动画，`prefers-reduced-motion` 已兜底）；`lyCardSVG` + `lyExportPNG`（SVG→Image→canvas→PNG，2× 缩放）；占卦本 `localStorage` 键 `ly_book_v1`（同卦同类别去重、上限 40、超配额自动砍半） |

### 10.2 本轮修掉的 3 个真问题

| ID | 级别 | 问题 | 证据 / 修法 |
|---|---|---|---|
| **E1** | P1 | **「占卦本」按钮点不到** —— 按钮在起卦页 `.ly-actions` 内，而一旦开始摇卦 `lyAsk` 就 `display:none`；结果页尾注却写「可从起卦页的『占卦本』回看」，可结果页**根本没有回起卦页的路径** | 探针实测 `#lyBookBtn` 为 `0×0`。修法：结果页按钮行新增 `#lyBookR`（`data-lybook`），起卦页那个保留；两处统一走 `document` **事件委托**（结果页按钮是动态渲染的，一次性 `.onclick` 绑定会失效）。尾注文案同步改 |
| **E2** | **P0** | **移动端 390px 下六爻内容横向溢出 22px** —— 全站**没有全局 `box-sizing:border-box`**，`.ly-card` 为 `width:100%` + `padding:14px 16px` + 1px 边框 ⇒ 内容盒 367 + 34 = **401**；`.ly-ask` / `.ly-cast` / `.ly-bk-item` 同病 | 探针：`#lyResult` 367 vs `#lyResult .ly-card` **401（right=412）**。修法：四类元素加 `box-sizing:border-box`，手机端补 `max-width:100%` |
| **E3** | P2 | **动爻标记被画两遍，且动爻条比静爻条短一截** —— `lyYaoRow` 把 `mark` 同时塞进 `.ly-ybar` 与 `.ly-yaol`：○/× 出现两次，且动爻条被标记挤掉约 20px 宽，与静爻条、变卦条对不齐 | 截图肉眼可见。修法：只保留条下方那一个标记，`.ly-ybar` 内不再塞 ⇒ 所有爻条等宽、`○/×` 恒在条下 |

**顺带一处语义清理**：「同卦另断」的类别 chip 原借用 `.ly-flag mid`（那是**进阶标记**的类），导致「统计进阶标记数量」会把 9 个类别 chip 一并算进去。已独立为 `.ly-recast` / `.ly-recasts`。

### 10.3 复验结果

- **`python tests/test_liuyao.py` ⇒ 94 PASS / 0 FAIL**（含 64 卦 × 6 爻的 `zhouyi` 完整性自检）。
- **`node tools/uicheck.mjs`（`UI_BASE=http://127.0.0.1:8012`）⇒ 32 PASS / 0 FAIL，退出码 0，无运行期 JS 异常；连跑两次逐项一致。**
- 新增断言覆盖：一键摇卦直出结果页 / 卦盘结构（连线数 = 动爻数）/ 术语可点（六亲·旺衰）/ 同卦另断（点击后类别与用神随换、卦体不变）/ 第二档卡片（神煞·应期·卦爻辞，与 `localStorage` 存档交叉核对）/ 占卦本开合与复看 / 导出长图 SVG→PNG 全链路 / 手机端卦盘自适应与入口可点。

### 10.4 回归脚本又长了两个教训（§9.5 的续篇）

1. **一键摇卦的卦局，前端改不了。** 逐掷摇卦由 `lyRand()` 决定，可以反推 18 个布尔值做确定性驱动；但 `#lyQuick` 是**后端 `auto_cast`** 掷的，前端注入无效。原断言 `yaoRows === 12`（本卦 6 + 变卦 6）只在「有动爻」时成立，**摇出六爻皆静就假红**（实测 `bars:11 / yaoRows:6`）。改为断言不变量：`yaoRows === guaBoxes × 6`、`links === moves`、`bars ≥ 6`。
2. **`clickSel` 的可见性判据只看视口，没看祖先 `overflow` 裁剪。** 桌面 `#liuyaoPanel` 是 `max-height:86vh; overflow-y:auto` 的**内滚容器**（B2 只把 ≤720px 改成单层滚动），被它裁掉的元素 `getBoundingClientRect()` 仍在视口内 ⇒ 旧判据认为「可见」而不滚动，点击便打在**别处**（「点占卦本条目可复看」RUN1 假红、RUN2 通过）。修法：`clickSel` 增加 `elementFromPoint` **命中复核**，命中不到才 `scrollIntoView` 并重测；已能命中的元素仍不加延时，以保住 B10 的时序严格度。另外每次跑前 `removeItem('ly_book_v1')`——占卦本跨次累积会让列表越来越长，把上面这条的概率放大。

> **教训一句话**：`rect` 在视口内 ≠ 点得到。**滚动容器可以嵌套**，判定「能不能点」的权威是 `elementFromPoint`，不是几何。

**遗留（未做）**：桌面端 `#liuyaoPanel`（`max-height:86vh; overflow-y:auto`）与 `#funcpage` 仍是**双层滚动**——B2 当时只处理了 ≤720px，桌面沿用嵌套内滚（本次 E2 的假红即由此暴露）。是否统一成单层滚动，待与「桌面面板是否该内滚」一并决策。

---

## 11. 卦爻辞白话层 + 一键复制文本（2026-09-18 第三轮）

> 这一轮也不在 §1~§8 的 UI 评审清单内，而是**六爻模块的「可读性 / 可带出」补完**。立场依旧：**只做装卦与事实提取，不断吉凶**。

### 11.1 缘起：朋友说「先套卦辞」，盘完发现一半已经做了

用户请教了一位懂周易的朋友，建议是：**先「套卦辞」——把周易中对应卦象的解释预先准备好，形成一个门槛较低的上下文**，再看是继续走 AI 结合问询内容综合解读，还是作为结果直接导出去问别人。

盘点后确认：**原文早就进库了**（§10 已落地 `api/zhouyi.py`：64 卦辞 + 384 爻辞 + 卦级象义/大象）。朋友说的一半已经做完。真正缺的是三条：

| # | 缺口 | 为什么是缺口 |
|---|---|---|
| ① | **爻级白话 384 条全空** | 只有**卦级**有「象义」，**爻级**仍是文言（「潜龙勿用」之类）。外行读卦级看得懂、读爻辞读不动，而六爻恰恰是**看动爻**的 |
| ② | **本经与装卦混在同一视觉流里** | 读者分不清「这是周易原话」还是「这是纳甲装卦的推演」，容易误当成一套互相印证的体系 |
| ③ | **结果出不去** | 想拿去问别人只能截图，没有可复制的纯文本 |

### 11.2 落地清单

| # | 交付 | 实现落点 |
|---|---|---|
| ① | **补爻级白话（448 条）** | 源：`data/zhouyi_plain_p1..p4.py`（按八宫拆四批）；构建：`data/build_zhouyi_plain.py`（校验后合并）；产物：`api/zhouyi_plain.py`（**生成物，勿手改**，头部已注明） |
| ② | **卦爻辞卡分两层** | 默认层 = 白话（卦级 + 爻级）+ 徽标 `.ly-badge`「本经视角 · 非六爻判断」；折叠层 = 原文（`<details>` 默认收起）。与装卦卡**共用同一套层级语言、徽标相反**（装卦标「纳甲视角」） |
| ③ | **一键复制文本** | `#lyCopy`（置于 `#lySave` 旁）+ `lyCastText(r)` / `lyLegacyCopy(txt)` / `lyCopyText(r)`；长图 `lyCardSVG` 同步补白话并改页脚 |

**①的构建链是一条防线，不是一次性脚本。** `build_zhouyi_plain.py` 的校验规则（任一不过 ⇒ **直接拒绝写文件**）：

- 卦名集合与 `api/zhouyi.py` 完全一致（`MISSING` / `EXTRA` 都报错）；
- 每卦 `yao` 数恰为 6；
- 单条白话语义长度 ≥ 8 字（**这条一开始就抓到 4 条写太短**，见 11.4）；
- 白话不得与原辞逐字相同（防「白话」其实就是复制原文）。

产物自带 `self_check()`，且 `api/zhouyi.py` 的 `self_check()` 已扩展为**同时校验白话完整性** ⇒ 白话说漏了卦，`python tests/test_liuyao.py` 会直接红，不必靠人眼巡。规模：64 × (1 + 6) = **448 条，白话合计 9,961 字**。

**写入原则**（已写进四个源文件的头部注释）：**只做翻译与意象说明，不含断语、不判断用户所问之事**。原辞里本就带吉凶的（如「征凶」「悔亡」），白话只**复述其情境**，不追加新判断。

**③的两个设计取舍：**

1. 剪贴板优先 `navigator.clipboard.writeText`，失败回落 `lyLegacyCopy`（隐藏 `textarea` + `execCommand('copy')`）——非安全上下文 / 老浏览器兜底。
2. **同时把正文写进 `window.__lyText`**（`renderLiuyao` 里另挂 `window.__lyResult` 存整份结果）。这样**回归脚本能直接断言导出内容**，不必去读系统剪贴板（CDP 读剪贴板要额外权限、且不稳）。这是「为可测性留一个只读出口」的典型用法。

### 11.3 复验结果

- **`python tests/test_liuyao.py` ⇒ 94 PASS / 0 FAIL**（含 `zhouyi` 64 卦 × 6 爻 + 白话完整性自检）。
- **`node tools/uicheck.mjs`（`UI_BASE=http://127.0.0.1:8013`）⇒ 34 PASS / 0 FAIL，退出码 0，无运行期 JS 异常。**
- 断言由 32 项 → **34 项**，新增两条：
  - `5b 新增·卦爻辞白话默认可见、原文默认折叠、标明本经视角`
  - `5c 新增·一键复制文本`
- 第二档卡片的断言标题随文案由 `卦爻辞 · 通行本` 改为 `卦爻辞 · 周易本经`（卡片内标题同步改，断言跟着改）。
- 截图证据：`_shots/09-liuyao-plain-desktop.jpg`、`10-liuyao-plain-open-desktop.jpg`、`11-liuyao-btns-desktop.jpg`。

### 11.4 教训（§9.5 → §10.4 → 本篇，第三续）

1. **`js()` 会把异常吞成 `"__ERR__"`。** 第一轮回归只报「一键复制文本 — `__ERR__`」，完全不知道挂在哪。给 `js()` 补上回传 `exceptionDetails.description` 之后，才看见真因是 `SyntaxError: Invalid or unexpected token`。**「静默兜底」的代价是丢失诊断信息**——兜底可以留，但要把原始错误一并带出来。
2. **同一条表达式：Node 里本地校验通过、单独发给 CDP 也通过，只有套进回归的 `js()` 包装器才炸。** 原因是**转义层级叠加**（包装器本身是模板字符串，内层又含中文与 `\n` 转义）。三个探针脚本交叉验证后确认与产品无关。**结论：凡是要在 CDP 里求值的表达式，只允许它返回原样字符串或 JSON；一切字符串比较与拼装一律挪到 Node 侧。** 5c 改成「页面只 `return String(window.__lyText||'')`，Node 负责比对」后稳定通过。
3. **自己写错的断言，比产品 bug 更常见。** 原以为复制文本里恒有「初爻原文」，实际**只收动爻的原文**；`rows` 计数把「参断要点」里的「旺衰」也算进去了（得 7）。改为：按 `^(初爻|二爻|三爻|四爻|五爻|上爻)` 精确计数（恒为 6），并改为断言「**动爻**原文存在」。**断言失败先怀疑断言本身**——这一轮里它是唯一一次假红。

### 11.5 未决：AI 到底上不上（**本轮刻意没做**）

朋友建议的后半段（「继续走 AI 结合问询内容综合解读」）**没有做**，这是有意的：①②③ 的意义恰恰是**先把不用 AI 就能做到的部分做到位**，这样后面才有基准去判断 AI 究竟加了多少价值。同时，这一轮也把「上下文」这件事从模型手里拿了回来——**查表 ≫ 模型记忆**：预置白话不可能编造爻辞，而让模型凭记忆复述六十四卦 384 爻，错一条就是硬伤。

若后续要上 AI，先前评估的三条工程约束仍然成立，且**不是可以后补的**：

| 约束 | 为什么不能事后补 |
|---|---|
| AI 必须是**旁路**（async bypass），不挂进装卦主链 | 否则「装卦能不能出结果」被「模型可不可用」绑架，确定性被外包 |
| **密钥不落前端** | 落到前端 = 公开；后端代持并限流 |
| 喂给模型的**只能是后端算好的结构化事实** | 结构化契约里没有的字段，模型不得回填 ⇒ 从机制上堵死「编造爻辞 / 编造神煞」 |

> 也就是说：①②③ 不只是「锦上添花」，它们同时是 AI 方案的地基（事实层干净、可导出、可对照）。**先把地基浇了再决定要不要盖楼，顺序是对的。**

---

*§1~§8 为独立评审记录（首评时未改动任何源文件）；§9 为第一批 + 第二批的落地与复验记录；§10 为六爻模块「一二三档」功能纵深及其收尾修复；§11 为卦爻辞白话层与「一键复制文本」的补完（含 AI 上线的三条前置约束）。文中行号对应 2026-09-16 的 `index.html` 快照，后续改动会使行号漂移——**按选择器 / 变量名检索更稳**。*
